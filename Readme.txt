UE19CS333 - Image Processing and Computer Vision Project

The emotions.py file contains the main code to preprocess the data, train on dataset generate weights and predict the output.
The train_model.h5 is the weights file generated upon training on the dataset.
THe haarcascade_frontalface_default is the pretrained model for face detection.

To train on dataset - python emotion.py --mode train
To run use command - python emotions.py --mode display
